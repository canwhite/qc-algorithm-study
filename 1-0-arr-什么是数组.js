/*
什么是数组
1.数据按顺序存储在内存的连续空间内
2.由于是存储在连续空间内的，所以可以通过下标获取
3.如果要增加和删除数据，数组的操作就比链表复杂
    (1)如果要要添加数据到第二个位置，就要先在末尾确保需要增加的存储空间，
    然后为了给新数据腾出位置，就需要把已有数据一个个移开，最后在空出的位置写入数据，这样才算添加完数据
    (2)如果向删除第二个数据呢？首先删除目标数据，然后把后边的数据一个个往空位移，最后再删除多余空间
*/